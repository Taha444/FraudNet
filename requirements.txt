fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
email-validator==2.1.1        # required by pydantic's EmailStr
xgboost==2.0.3
pandas==2.3.3
joblib==1.4.2
python-multipart==0.0.9
numpy==1.26.4
scikit-learn==1.7.2

# Database
sqlalchemy==2.0.30
# PostgreSQL driver. SQLite needs no driver, so this is only used when
# DATABASE_URL points at PostgreSQL — SQLAlchemy imports it at create_engine
# time and fails immediately if it is missing.
psycopg2-binary==2.9.12

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Rate limiting
slowapi==0.1.9

# Explainability (optional — removes gracefully if not installed)
shap==0.45.0

# Tests — run with: pytest  (from the repository root)
pytest==8.2.0
httpx==0.27.0                 # required by fastapi's TestClient
